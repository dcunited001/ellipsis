$wsTui=tuitray
$wsTuiClass=org.dc.tuitray
$wsTuiPrefix=tuitray
$wsTuiKey=Prior # MOD3+pageup <PAUS> on desktop

windowrule = workspace special:$wsTui, match:class ^(Alacritty:$wsTuiClass)$
windowrule = float 1, match:class ^(Alacritty:$wsTuiClass)$

bind = MOD3, $wsTuiKey, togglespecialworkspace, $wsTui
bind = MOD3 SHIFT, $wsTuiKey, movetoworkspacesilent, special:$wsTui

# sysz
windowrule = size 1024 768, match:class ^(Alacritty:$wsTuiClass)$, match:title ($wsTuiPrefix:sysz)
#windowrule = move 5% 5% match:class ^(Alacritty:$wsTuiClass)$, match:title ($wsTuiPrefix:sysz)
windowrule = move 5% 100%-w-5%, match:class ^(Alacritty:$wsTuiClass)$, match:title ($wsTuiPrefix:sysz)

# btop
windowrule = size 75% 75%, match:class ^(Alacritty:$wsTuiClass)$, match:title ($wsTuiPrefix:btop)
windowrule = move 100%-w-5% 100%-w-5%, match:class ^(Alacritty:$wsTuiClass)$, match:title ($wsTuiPrefix:btop)

# nmtui
windowrule = size 1024 768, match:class ^(Alacritty:$wsTuiClass)$, match:title ($wsTuiPrefix:nmtui)
windowrule = move 100%-w-5% 5%, match:class ^(Alacritty:$wsTuiClass)$, match:title ($wsTuiPrefix:nmtui)

# ---------------------------------------------
# Matching class/title

# $wsTuiClass=Alacritty,$instance
#
# on wayland, the instance class is ignored
#
# - Terminal apps will interpret terminfo, app preferences, etc to perform
#   formatting using %s variables, so you lose control of the title.
#
# - Here, it doesn't matter, since alacritty execs a bin. but if the process
#  starts under an interactive shell it does
#
# - If Alacritty isn't in the class, you lose the ability to identify it as an
#   Alacritty app

# ---------------------------------------------
#
# - the hyprlang param:val may up a colon $wsTuiClass=Alacritty:org.dc.tuitray,
#   but this doesn't appear to happen (i was launching with dc.org.tuitray...)
#
# - otherwise I would select with ".": match:class (Alacritty).$wsTuiClass

# that enables Alacritty applications to still be selected by match:class ^(Alacritty).*
