# by class
windowrule = float, class:^(wpical)(.*)$
windowrule = float, class:^(edu.wpi.first.pathweaver)(.*)$
windowrule = float, class:^(edu-wpi-first-smartdashboard)(.*)$
windowrule = float, class:^(edu.wpi.first.shuffleboard)(.*)$
windowrule = float, class:(Datalog Tool)
windowrule = float, class:(elastic_dashboard)

# by title
windowrule = float, title:(Glass -)(.*)$
windowrule = float, title:(System Identification)(.*)$
windowrule = float, title:(OutlineViewer -)(.*)$
windowrule = float, title:(roboRIO Team Number)(.*)$

# ---------------------------------------------
# Robot Simulation

$wsFrcRobotSim=frc-sim
$wsFrcRobotSimKey=73      # F7
$wsFrcRobotSimClass=Robot Simulation
# omarchy: can't find zenity/matedialog/qarma/kdialog

workspace = $wsFrcRobotSim, monitor:$m2port, layoutopt:orientation:bottom, mfact: 0.4
bind = SUPER, code:$wsFrcRobotSimKey, workspace, $wsFrcRobotSim
bind = SUPER SHIFT, code:$wsFrcRobotSimKey, movetoworkspacesilent, $wsFrcRobotSim

# windowrule = tag +frcsim, 
windowrule = tile, class:^(Robot Simulation)$, title:^(Robot Simulation)$

# ---------------------------------------------
# Advantage Scope

# $wsFrcAscope=frcascope
$wsFrcAscope=frc-ascope
$wsFrcAscopeKey=74      # F8
$wsFrcAscopeFloat=frc-ascopefloat
$wsFrcAscopeFloatKey=75 # F9

workspace = $wsFrcAscope, monitor:$m2port, layoutopt:orientation:center, mfact: 0.4
bind = SUPER, code:$wsFrcAscopeKey, workspace, $wsFrcAscope
bind = SUPER SHIFT, code:$wsFrcAscopeKey, movetoworkspacesilent, $wsFrcAscope

workspace = $wsFrcAscopeFloat, monitor:$m2port, layoutopt:orientation:center, mfact: 0.4
bind = SUPER, code:$wsFrcAscopeFloatKey, workspace, $wsFrcAscopeFloat
bind = SUPER SHIFT, code:$wsFrcAscopeFloatKey, movetoworkspacesilent, $wsFrcAscopeFloat

# windowrule = float, title:(AdvantageScope)
windowrule = tag +ascope, title:(AdvantageScope)(.*)
# windowrule = float, class:(AdvantageScope)(.*)

# tags, but doesn't float the window
$ascopeSatSuffix=( @ — AdvantageScope)
windowrule = tag +ascopeSatellite, title:(.*)$ascopeSatSuffix(.*)
windowrule = tile, title:(.*)$ascopeSatSuffix(.*) # req to float windows
windowrule = float, tag:ascopeSatellite*          # doesn't float any wins
windowrule = bordercolor rgba(33dd33FF) / rgba(bb33aaAA) 45deg, tag:ascopeSatellite*
windowrule = bordersize 5px, tag:ascopeSatellite*
windowrule = renderunfocused, tag:ascopeSatellite*
windowrule = workspace special:$wsFrcAscopeFloat, tag:ascopeSatellite*

# NOTE: renderunfocused is necessary, otherwise AdvantageScope graphs
# offscreen will cause the main interface to lag (lol lucky guess? nope)

# hyprctl clients -j | jq 'map(select(IN(.class; "AdvantageScope")))'
# hyprctl clients -j | jq 'map(select(IN(.tag; "ascopeSatellite")))'  # ... still not selecting

# ascope popouts get a title (1 per type of graph)

# ---------------------------------------------
# Robot Builder
#
# starts offscreen, but move doesn't work for some reason
windowrule = float, class:(robotbuilder-RobotBuilder)
windowrule = move 5% 5%, class:(robotbuilder-RobotBuilder)
